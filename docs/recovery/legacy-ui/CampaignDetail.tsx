import { ArrowLeft, ExternalLink, Upload } from "lucide-react";
import { useState } from "react";
import { Link, useRoute } from "wouter";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";

export default function CampaignDetail() {
  const [, params] = useRoute("/campaign/:id");
  const [clipUrl, setClipUrl] = useState("");
  const [viewsImage, setViewsImage] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const campaigns = trpc.klipflow.bounties.useQuery();
  const campaign = campaigns.data?.find((item) => item.id === params?.id);
  if (campaigns.isLoading) return <div className="rounded-3xl bg-white p-8 text-center text-black/50">Loading campaign…</div>;
  if (!campaign) return <div className="space-y-4"><Link href="/"><button className="flex items-center gap-2 text-sm font-bold text-black/50"><ArrowLeft size={17} /> Back</button></Link><div className="rounded-3xl bg-white p-8 text-center"><h1 className="text-xl font-black">Campaign not found</h1><p className="mt-2 text-sm text-black/45">This campaign may have ended.</p></div></div>;
  return <div className="space-y-5"><Link href="/"><button className="flex items-center gap-2 text-sm font-bold text-black/50"><ArrowLeft size={17} /> Back to campaigns</button></Link><div className="rounded-3xl bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-[#FFE5D9] font-bold text-[#FF4D00]">{campaign.creatorName?.[0] ?? "C"}</div><div><h1 className="text-xl font-black">{campaign.title}</h1><p className="text-sm text-black/45">{campaign.platform}</p></div></div><div className="mt-5 rounded-2xl bg-[#FFF1EB] p-4"><div className="text-sm text-black/50">You earn</div><div className="text-3xl font-black text-[#FF4D00]">{campaign.ratePerThousand ?? campaign.reward}</div><div className="text-sm text-black/50">for every 1,000 views</div></div></div><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-lg font-black">How to make your clip</h2><ol className="mt-4 space-y-4 text-sm"><li className="flex gap-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-[#FF4D00] font-bold text-white">1</span><span>Watch the source video and make your own short clip.</span></li><li className="flex gap-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-[#FF4D00] font-bold text-white">2</span><span>Add captions, a strong first second, and your own style.</span></li><li className="flex gap-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-[#FF4D00] font-bold text-white">3</span><span>Post it, then send us the link and a screenshot of your views.</span></li></ol>{campaign.sourceUrl && <a href={campaign.sourceUrl} target="_blank" rel="noreferrer" className="mt-5 flex h-11 items-center justify-center gap-2 rounded-2xl border border-black/10 text-sm font-extrabold"><ExternalLink size={17} /> Watch source video</a>}</section><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-lg font-black">Upload my clip</h2><p className="mt-1 text-sm text-black/45">Paste your TikTok, Instagram, or YouTube link.</p>{submitted ? <div className="mt-5 rounded-2xl bg-[#FFF1EB] p-4 text-sm font-semibold text-[#B63D00]">Your clip was sent! We will check it and update you here.</div> : <div className="mt-4 space-y-3"><Input value={clipUrl} onChange={(e) => setClipUrl(e.target.value)} placeholder="Paste your clip link" className="h-12 rounded-2xl border-black/10 bg-[#F7F7F5] text-base" /><label className="flex min-h-24 cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed border-black/10 text-center text-sm text-black/45"><Upload size={20} /><span className="mt-1">Add a screenshot of your views</span><input type="file" accept="image/*" className="hidden" onChange={(e) => setViewsImage(e.target.files?.[0]?.name ?? "")} /></label>{viewsImage && <p className="text-xs text-black/45">Added: {viewsImage}</p>}<Button disabled={!clipUrl || !viewsImage} onClick={() => setSubmitted(true)} className="h-12 w-full rounded-2xl bg-[#FF4D00] text-sm font-extrabold text-white hover:bg-[#E64500]">Submit for review</Button></div>}</section></div>;
}
